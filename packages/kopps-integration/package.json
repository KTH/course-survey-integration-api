{
  "name": "kopps-integration",
  "description": "Abstraction layer for integration with KOPPS",
  "private": true,
  "scripts": {
    "test:lint": "prettier --check . && tsc --noEmit",
    "test:unit": "jest __tests__/.+\\.test\\.ts",
    "test": "npm run test:lint && npm run test:unit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "got": "^11.8.6"
  }
}
